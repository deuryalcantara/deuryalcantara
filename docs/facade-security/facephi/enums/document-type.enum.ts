// src/facephi/enums/document-type.enum.ts
//
// Sin cambios respecto a la versión base de Daniel. Se incluye por completitud:
// los valores deben coincidir con los que valida el microservicio (CED | PASSPORT).

export enum DocumentType {
  CED = 'CED',
  PASSPORT = 'PASSPORT',
}
